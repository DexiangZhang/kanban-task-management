import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Kanban Dashboard || Task Detail",
  description: "Single Task Detail",
};

export default Page;
